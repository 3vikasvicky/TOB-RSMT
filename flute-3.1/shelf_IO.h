void writeBranchFile(char outputDir[], Tree t)  
        /* -----------------------------------------------------------------------------
                   Reads the .blocks file to get blocks region information by Yilin
                     
                           creates extern vars:
                                       numBlocks, maxBlocksDegree, xBlocksCord, yBlocksCord;
                                           -------------------------------------------------------------------------------- */
        extern void readBlocksFile (char benchmarkPath[], char blocksFile[]);

